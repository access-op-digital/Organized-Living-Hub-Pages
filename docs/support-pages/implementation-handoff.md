# Implementation handoff

Read `DESIGN.md`, `design-contract.md`, `../bedroom/DESIGN.md`, the existing
`bedroom-closet.css` / `.js`, and the three reference HTML/JSON source files.

Create root-level `faq.html`, `testimonials.html`, `gallery.html`, plus a shared
support stylesheet and FAQ interaction script. Use existing static deployment
conventions and source-recorded content. Preserve quotations and project records.
Apply the documented commercial-voice and factual corrections explicitly.

Use existing brand assets only. Do not label generic storage photography as
project evidence. Collapse grids on mobile and make the long topic rail usable
without blocking access to the FAQ content. Keep all answers in HTML.

The first artifact must prove the shared branded header and hero, the complete
FAQ topic/disclosure structure, and the testimonials/gallery card hierarchy.
Link all three pages from the catalog and the collection's footer navigation.
